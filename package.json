{
  "name": "@radatek/microdb",
  "version": "1.0.6",
  "description": "Lightweight plain file store database with MongoDB interface",
  "author": "Darius Kisonas",
  "license": "MIT",
  "keywords": [
    "nosql",
    "embedded",
    "database",
    "microdb",
    "mongodb"
  ],
  "type": "module",
  "main": "microdb.ts",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/radateklt/microdb.git"
  },
  "scripts": {
    "prepublishOnly": "pnpm build && echo 'call: pnpm run publish' && exit 1",
    "build": "pnpm --package typescript dlx tsc -d -m nodenext -t es2021 microdb.ts --outDir dist && sed -Ei 's/\\\"use strict\\\";\\n\\n//g' dist/microdb.js && sed -Ei ':a;N;$!ba;s/\\n +private _[^\\n]+//g;s/export \\{\\};//g' dist/microdb.d.ts; cp package.json dist/package.json && sed -Ei ':a;N;$!ba;s|\"main\"[^\\n]+|\"main\": \"microdb.js\",\\n  \"module\": \"microdb.js\",\\n  \"types\": \"microdb.d.ts\",|;s|,\\n  \"scripts\".*$|\\n}|' dist/package.json",
    "test": "node test.ts",
    "publish": "pnpm publish dist --access public"
  },
  "devDependencies": {
    "@types/node": "^24.10.1"
  }
}
